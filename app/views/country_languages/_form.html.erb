<%= form_with(model: country_language) do |form| %>
  <% if country_language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country_language.errors.count, "error") %> prohibited this country_language from being saved:</h2>

      <ul>
        <% country_language.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :country_id %>
    <%= form.select :country_id, Country.all.collect { |country| [ country.name, country.id ] }, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :language_id %>
    <%= form.select :language_id, Language.all.collect { |language| [ language.name, language.id ] }, include_blank: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
